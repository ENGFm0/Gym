rules_version = '2';

// The API reaches Firestore with admin credentials, so the browser needs no access at all.
// Anything that can read a member's data goes through an endpoint that checks who is asking.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
